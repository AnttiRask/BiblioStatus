- Sidebar
    - Now the toggle sidebar is on top of the buttons and dropdown menus. Could we have a slightly smaller margin on the left side and move everything to the left so that there's enough space 
    for the toggle sidebar arrow.
    - Clear All Selections still doesn't 'clear' the map and return it to the same state where it was after we opened the app and chose to use the current city
    - If I've selected a service, it's cool that selecting city/municipality only shows those places that have that service. But if I switch to another city, the Select Service switches back to All Services (although the service is still clearly selected, because it still shows only those citys with the service). I would expect the text to still say the selected service

- Service Statistics
    - The texts for the visualization in dark mode are too dark. I asked you to use the same color as for the other text on that page!
    - I don't know what happens in the light mode, but the texts are a weird shade of green(?). Again, they should be the same color as the other text on that page.
